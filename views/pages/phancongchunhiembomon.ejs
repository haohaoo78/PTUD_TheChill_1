<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Phân công giáo viên</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/sweetalert2@11" />
<style>
  body {
    font-family: Arial, sans-serif;
    background: #f4f7fb;
    margin: 0;
    color: #1a2a44;
  }
  header {
    background: #0a1d37;
    padding: 20px 0;
    text-align: center;
    color: #fff;
    font-size: 26px;
    font-weight: bold;
  }
  main {
    padding: 20px 40px;
  }
  .controls label {
    font-weight: bold;
    margin-right: 5px;
  }
  select {
    padding: 6px 10px;
    border-radius: 6px;
    background: white;
    border: 1px solid #b8c2d1;
    margin-right: 15px;
    font-size: 14px;
  }
  .mode-tabs {
    margin-top: 10px;
  }
  .mode {
    padding: 8px 18px;
    border: none;
    background: #1b2d4f;
    color: #fff;
    border-radius: 6px;
    margin-right: 8px;
    cursor: pointer;
    transition: 0.25s;
  }
  .mode.active,
  .mode:hover {
    background: #274374;
  }
  .panel {
    background: #fff;
    padding: 20px;
    margin-top: 20px;
    border-radius: 10px;
    box-shadow: 0 3px 10px rgba(0,0,0,0.1);
  }
  .hide { display: none; }
  table.grid {
    width: 100%;
    border-collapse: collapse;
    margin-top: 15px;
  }
  table.grid th {
    background: #0a1d37;
    color: #fff;
    padding: 10px;
  }
  table.grid td {
    padding: 10px;
    border-bottom: 1px solid #d9e2f1;
    background: #fff;
  }
  .actions button,
  #load-classes,
  #load-bomon-classes,
  #assign-bomon {
    background: #0a1d37;
    color: #fff;
    border: none;
    padding: 10px 18px;
    border-radius: 6px;
    cursor: pointer;
    transition: 0.25s;
  }
  button:hover {
    background: #1b3a61;
  }
  /* Modal */
  .modal {
    display: none;
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(0, 0, 0, 0.4);
  }
  .modal-content {
    background: #fff;
    margin: 10% auto;
    padding: 20px;
    border-radius: 10px;
    width: 50%;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
  }
  .close {
    float: right;
    font-size: 24px;
    cursor: pointer;
  }
</style>
</head>
<body>
<header>
  Phân công giáo viên
</header>
<main>
  <section>
    <div class="controls">
      <label>Năm học:</label>
      <select id="namhoc-select"><option>2024-2025</option></select>

      <label>Học kỳ:</label>
      <select id="kyhoc-select"><option>1</option></select>

      <div class="mode-tabs">
        <button id="tab-chunhiem" class="mode active">Chủ nhiệm</button>
        <button id="tab-bomon" class="mode">Bộ môn</button>
      </div>
    </div>

    <div id="chunhiem-panel" class="panel">
      <h3>Phân công giáo viên chủ nhiệm</h3>
      <div class="actions">
        <button id="load-classes">Tải danh sách lớp</button>
      </div>
      <table id="classes-table" class="grid">
        <thead>
          <tr><th>STT</th><th>Mã lớp</th><th>Tên lớp</th><th>Giáo viên chủ nhiệm</th><th>Thao tác</th></tr>
        </thead>
        <tbody>
          <tr><td colspan="5" class="text-center">Chưa có dữ liệu</td></tr>
        </tbody>
      </table>
    </div>

    <div id="bomon-panel" class="panel hide">
      <h3>Phân công giáo viên bộ môn</h3>
      <div class="selectors">
        <label>Khối:</label>
        <select id="khoi-select"></select>
        <label>Môn:</label>
        <select id="mon-select"></select>
        <label>Giáo viên:</label>
        <select id="gv-select"></select>
        <button id="load-bomon-classes">Tải lớp</button>
      </div>
      <table id="classes-bomon-table" class="grid">
        <thead>
          <tr><th>STT</th><th>Mã lớp</th><th>Tên lớp</th><th>Chọn</th></tr>
        </thead>
        <tbody>
          <tr><td colspan="4" class="text-center">Chưa có dữ liệu</td></tr>
        </tbody>
      </table>
      <div class="actions">
        <button id="assign-bomon">Phân công bộ môn</button>
      </div>
    </div>
  </section>
</main>

<div id="assign-modal" class="modal">
  <div class="modal-content">
    <span class="close">&times;</span>
    <h3>Phân công giáo viên</h3>
    <div id="modal-body"></div>
  </div>
</div>

<script src="/js/PhanCongChuNhiemBoMon.js"></script>
</body>
</html>
