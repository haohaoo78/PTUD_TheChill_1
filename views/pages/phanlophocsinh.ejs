<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Phân lớp học sinh</title>
  <link rel="stylesheet" href="/css/phanlop.css">
</head>
<body>
  <div class="phanlop-container">
    <div class="page-header">
      <h2>📚 Phân lớp học sinh</h2>
      <p class="subtitle">Quản lý phân chia học sinh vào các lớp theo khối và tổ hợp môn</p>
    </div>

    <!-- Controls -->
    <div class="controls">
      <div class="control-group">
        <label for="khoi-select">Khối:</label>
        
        <select id="khoi-select" class="form-select">
          <% khoiList.forEach(k => { %>
            <option value="<%= k.MaKhoi %>" <%= (k.MaKhoi === selectedKhoi) ? 'selected' : '' %>>
              <%= k.TenKhoi %>
            </option>
          <% }) %>
        </select>
        <small class="help-text">Chọn khối để phân lớp</small>
      </div>
      <div class="control-group">
        <label for="max-size">Sĩ số tối đa:</label>
        <input type="number" id="max-size" class="form-input" min="20" max="50" value="35">
        <small class="help-text">Từ 20-50 học sinh</small>
      </div>
      <div class="control-buttons">
        <button id="load-students" class="btn btn-primary">
          <span class="btn-icon">🔄</span> Tải dữ liệu
        </button>
        <button id="auto-assign" class="btn btn-secondary">
          <span class="btn-icon">⚡</span> Phân bổ tự động
        </button>
        <button id="save-assign" class="btn btn-success">
          <span class="btn-icon">💾</span> Lưu phân bổ
        </button>
      </div>
    </div>

    <!-- Main Content -->
    <div class="content-wrapper">
      <!-- Students Panel -->
      <section class="students-panel">
        <div class="panel-header">
          <h3>Danh sách học sinh</h3>
          <span class="count-badge" id="students-count">0 học sinh</span>
        </div>
        <div class="table-wrapper">
          <table id="students-table" class="data-table">
            <thead>
              <tr>
                <th width="50">STT</th>
                <th width="100">Mã HS</th>
                <th>Tên học sinh</th>
                <th width="100">Giới tính</th>
                <th width="150">Tổ hợp</th>
                <th width="120">Trạng thái</th>
                <th width="120">Lớp</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td colspan="7" class="empty-state">
                  <div class="empty-icon">📋</div>
                  <p>Nhấn "Tải dữ liệu" để xem danh sách</p>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </section>

      <!-- Classes Panel -->
      <section class="classes-panel">
        <div class="panel-header">
          <h3>Danh sách lớp</h3>
          <span class="count-badge" id="classes-count">0 lớp</span>
        </div>
        <div class="table-wrapper">
          <table id="classes-table" class="data-table">
            <thead>
              <tr>
                <th width="50">STT</th>
                <th width="100">Mã lớp</th>
                <th>Tên lớp</th>
                <th width="100">Sĩ số</th>
                <th width="150">Hiện tại</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td colspan="5" class="empty-state">
                  <div class="empty-icon">🏫</div>
                  <p>Chưa có dữ liệu lớp</p>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
        <p class="table-note">💡 Double click vào lớp để xem chi tiết học sinh</p>
      </section>
    </div>
  </div>

  <!-- Modal xem học sinh trong lớp -->
  <div id="class-modal" class="modal">
    <div class="modal-content">
      <div class="modal-header">
        <h3>Danh sách học sinh lớp <span id="class-modal-title"></span></h3>
        <button class="close-btn" onclick="closeModal('class-modal')">×</button>
      </div>
      <div class="modal-body" id="class-modal-body">
        <div class="loading-state">
          <div class="spinner"></div>
          <p>Đang tải...</p>
        </div>
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" onclick="closeModal('class-modal')">Đóng</button>
      </div>
    </div>
  </div>

  <!-- Modal chỉnh sửa lớp -->
  <div id="edit-modal" class="modal">
    <div class="modal-content modal-small">
      <div class="modal-header">
        <h3>Chỉnh sửa lớp học sinh</h3>
        <button class="close-btn" onclick="closeModal('edit-modal')">×</button>
      </div>
      <div class="modal-body">
        <div class="form-group">
          <label for="edit-class-select">Chọn lớp mới:</label>
          <select id="edit-class-select" class="form-select">
            <option value="">-- Bỏ phân lớp --</option>
          </select>
        </div>
      </div>
      <div class="modal-footer">
        <button class="btn btn-secondary" onclick="closeModal('edit-modal')">Hủy</button>
        <button id="edit-confirm" class="btn btn-primary">Xác nhận</button>
      </div>
    </div>
  </div>

  <!-- Loading Overlay -->
  <div id="loading-overlay" class="loading-overlay">
    <div class="loading-content">
      <div class="spinner-large"></div>
      <p>Đang xử lý...</p>
    </div>
  </div>

  <script src="/js/PhanLop.js"></script>
</body>
</html>