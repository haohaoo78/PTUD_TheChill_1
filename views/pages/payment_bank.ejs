<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh toán chuyển khoản</title>
    <link rel="stylesheet" href="/css/globals.css">
    <style>
        .bank-transfer-container {
            max-width: 600px;
            margin: 50px auto;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            text-align: center;
        }
        .qr-code {
            max-width: 300px;
            margin: 20px auto;
            border: 1px solid #ddd;
            padding: 10px;
            border-radius: 5px;
        }
        .qr-code img {
            width: 100%;
            height: auto;
        }
        .bank-info {
            text-align: left;
            margin: 20px 0;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 5px;
        }
        .bank-info p {
            margin: 8px 0;
            font-size: 16px;
        }
        .btn-confirm {
            background: #28a745;
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            margin-top: 20px;
        }
        .btn-confirm:hover {
            background: #218838;
        }
        .note {
            color: #6c757d;
            font-size: 14px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="bank-transfer-container">
        <h2>Thanh toán qua Ngân hàng</h2>
        <p>Vui lòng quét mã QR bên dưới để thanh toán</p>
        
        <div class="qr-code">
            <img src="<%= qrUrl %>" alt="VietQR Code">
        </div>

        <div class="bank-info">
            <p><strong>Ngân hàng:</strong> <%= bankInfo.bankId %></p>
            <p><strong>Số tài khoản:</strong> <%= bankInfo.accountNo %></p>
            <p><strong>Chủ tài khoản:</strong> <%= bankInfo.accountName %></p>
            <p><strong>Số tiền:</strong> <%= new Intl.NumberFormat('vi-VN').format(amount) %> VND</p>
            <p><strong>Nội dung:</strong> <%= content %></p>
        </div>

        <button id="btn-confirm" class="btn-confirm">Tôi đã chuyển khoản</button>
        
        <p class="note">Lưu ý: Sau khi chuyển khoản thành công, vui lòng nhấn nút "Tôi đã chuyển khoản" để hoàn tất.</p>
    </div>

    <script>
        document.getElementById('btn-confirm').addEventListener('click', async () => {
            if(!confirm('Bạn chắc chắn đã thực hiện chuyển khoản?')) return;

            const data = <%- JSON.stringify(transactionData) %>;
            
            try {
                const res = await fetch('/api/hocphi/confirm-bank-transfer', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                const result = await res.json();
                
                if (result.success) {
                    alert('Xác nhận thành công! Hệ thống sẽ cập nhật trạng thái.');
                    window.location.href = '/?page=hocphi';
                } else {
                    alert('Có lỗi xảy ra: ' + result.message);
                }
            } catch (err) {
                console.error(err);
                alert('Lỗi kết nối đến server');
            }
        });
    </script>
</body>
</html>