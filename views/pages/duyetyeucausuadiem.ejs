<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/DuyetYeuCauSuaDiem.css">
</head>
<body>


  <main_1>
    <section class="approve-section">
      <div class="page-header">
        <h2 class="page-title">DUYỆT YÊU CẦU SỬA ĐIỂM</h2>
        <p class="page-description">Xem xét và phê duyệt các yêu cầu sửa điểm từ giáo viên</p>

        <div class="filter-buttons">
          <button class="filter-btn active" data-status-code="DangXuLy">Đang xử lý</button>
          <button class="filter-btn" data-status-code="DaDuyet">Đã duyệt</button>
          <button class="filter-btn" data-status-code="BiTuChoi">Bị từ chối</button>
        </div>
      </div>

      <table id="requests-table" class="grid">
        <thead>
          <tr>
            <th>STT</th>
            <th>Mã yêu cầu</th>
            <th>Mã học sinh</th>
            <th>Tên học sinh</th>
            <th>Môn học</th>
            <th>Loại điểm</th>
            <th>Điểm cũ</th>
            <th>Điểm mới</th>
            <th>Lý do</th>
            <th>Giáo viên gửi</th>
            <th>Trạng thái</th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <% if(requests.length){ %>
            <% requests.forEach((r,i)=>{ %>
              <tr data-id="<%= r.MaYeuCau %>" data-trangthai="<%= r.TrangThai %>">
                <td><%= i+1 %></td>
                <td><%= r.MaYeuCau %></td>
                <td><%= r.MaHocSinh %></td>
                <td><%= r.TenHocSinh %></td>
                <td><%= r.TenMonHoc %></td>
                <td><%= r.LoaiDiem %></td>
                <td class="text-red"><%= r.DiemCu %></td>
                <td class="text-green"><%= r.DiemMoi %></td>
                <td><%= r.LyDo %></td>
                <td><%= r.TenGiaoVien %></td>
                <td data-status="<%= r.TrangThai %>" data-status-display="<%= r.TrangThai === 'DangXuLy' ? 'Đang xử lý' : (r.TrangThai === 'DaDuyet' ? 'Đã duyệt' : (r.TrangThai === 'BiTuChoi' ? 'Bị từ chối' : r.TrangThai)) %>">
                  <% if (r.TrangThai === 'DangXuLy') { %>Đang xử lý<% } %>
                  <% if (r.TrangThai === 'DaDuyet') { %>Đã duyệt<% } %>
                  <% if (r.TrangThai === 'BiTuChoi') { %>Bị từ chối<% } %>
                </td>
                <td>
                  <% if(r.TrangThai==='DangXuLy'){ %>
                    <button class="view-btn btn">Xem chi tiết</button>
                    <button class="approve-btn btn">✅ Duyệt</button>
                    <button class="reject-btn btn">❌ Từ chối</button>
                  <% } else { %>
                    <button class="view-btn btn">Xem chi tiết</button>
                    <% if(r.TrangThai==='BiTuChoi' && r.GhiChu){ %>
                      <button class="note-btn btn" title="<%= r.GhiChu %>">👁️ Lý do từ chối</button>
                    <% } %>
                    <% if(r.TrangThai==='DaDuyet'){ %>
                      <em class="status-done">✔️ Đã duyệt</em>
                    <% } %>
                  <% } %>
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr><td colspan="12" style="padding:40px; font-style:italic; color:#666;">Không có yêu cầu</td></tr>
          <% } %>
        </tbody>
      </table>

      <!-- Modal chi tiết -->
      <div id="detail-modal" class="modal">
        <div class="modal-content">
          <span class="close">&times;</span>
          <h3>Chi tiết yêu cầu</h3>
          <div id="detail-content"></div>
        </div>
      </div>

      <!-- Toast message -->
      <div id="toast-message"></div>
    </section>
  </main_1>

  <script src="/js/DuyetYeuCauSuaDiem.js"></script>
</body>
</html>
